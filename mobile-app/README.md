# Lunch Orders Mobile App (Android)

Android приложение за преглед на обедни поръчки от Google Sheets.

## 📋 Предварителни изисквания

1. **Node.js** (версия 18 или по-нова)
   - Изтеглете от: https://nodejs.org/

2. **Expo CLI**
   ```bash
   npm install -g expo-cli
   ```

3. **EAS CLI** (за билдване на APK)
   ```bash
   npm install -g eas-cli
   ```

4. **ExpoAccount**
   - Създайте акаунт на: https://expo.dev/
   - Логнете се: `eas login`

## 🚀 Инсталация и настройка

### Стъпка 1: Инсталиране на зависимостите

#### За мобилното приложение:
```bash
cd mobile-app
npm install
```

#### За backend сървъра:
```bash
cd mobile-app/backend
npm install
```

### Стъпка 2: Конфигуриране на backend сървъра

1. Отворете `mobile-app/backend/server.js`
2. Уверете се, че credentials са правилни (вече са добавени от съществуващата конфигурация)

### Стъпка 3: Конфигуриране на мобилното приложение

1. Отворете `mobile-app/services/googleSheets.js`
2. Променете `BACKEND_URL` с IP адреса на вашия компютър:
   ```javascript
   const BACKEND_URL = 'http://192.168.1.100:3001'; // Сменете с вашия IP
   ```

За да намерите IP адреса на вашия компютър:
- **Windows**: Отворете Command Prompt и напишете `ipconfig` (вземете IPv4 Address)
- **Mac/Linux**: Отворете Terminal и напишете `ifconfig` или `ip addr`

### Стъпка 4: Стартиране на backend сървъра

В терминал в папката `mobile-app/backend`:
```bash
npm start
```

Сървърът ще стартира на порт 3001. Уверете се, че е достъпен:
```bash
curl http://localhost:3001/api/health
```

### Стъпка 5: Тестване на приложението локално

1. Отворете нов терминал в папката `mobile-app`
2. Стартирайте Expo:
   ```bash
   npm start
   ```
3. Сканирайте QR кода с Expo Go приложението на телефона ви (изтеглете от Play Store)

## 📦 Билдване на APK файл за инсталация

### Метод 1: Използване на EAS Build (Препоръчителен)

1. **Инициализирайте EAS в проекта:**
   ```bash
   cd mobile-app
   eas build:configure
   ```

2. **Билдвайте APK файла:**
   ```bash
   eas build --platform android --profile production
   ```

3. Изчакайте билда да завърши (може да отнеме 10-15 минути)
4. Изтеглете APK файла от линка в конзолата или от https://expo.dev/
5. Прехвърлете APK файла на телефоните

### Метод 2: Local Build (Без Expo облак)

Ако искате да билдвате локално без да използвате Expo облака:

1. **Инсталирайте Android Studio** (за Android SDK)
2. **Генерирайте местен билд:**
   ```bash
   npx expo run:android --variant release
   ```

## 📱 Инсталиране на APK на телефоните

### Начин 1: USB кабел
1. Свържете телефона към компютъра
2. Копирайте APK файла в телефона
3. Отворете файловия мениджър на телефона
4. Намерете и отворете APK файла
5. Разрешете "Install from unknown sources" ако се изисква
6. Инсталирайте приложението

### Начин 2: Google Drive / Dropbox
1. Качете APK файла в Google Drive или Dropbox
2. Отворете линка на телефона
3. Изтеглете и инсталирайте APK файла

### Начин 3: Email
1. Прикачете APK файла към email
2. Отворете email-а на телефона
3. Изтеглете прикачения файл
4. Инсталирайте APK файла

## 🔧 Конфигурация на Production

### Важно: Backend хостинг

За да работи приложението извън локалната мрежа, backend сървърът трябва да бъде достъпен онлайн.

**Опции за хостинг на backend:**

1. **Render.com** (Безплатно)
   - Създайте акаунт на https://render.com
   - Deploy backend папката като Web Service
   - Обновете `BACKEND_URL` в `services/googleSheets.js` с вашия Render URL

2. **Railway.app** (Безплатно ниво)
   - https://railway.app/
   - Deploy backend като Node.js приложение

3. **Heroku** (Платено, но надеждно)
   - https://heroku.com/

4. **Собствен сървър**
   - Ако имате собствен сървър или VPS
   - Уверете се, че порт 3001 е отворен

### Актуализиране на BACKEND_URL след deploy

След като deploy-нете backend, обновете URL в приложението:

1. Отворете `mobile-app/services/googleSheets.js`
2. Променете:
   ```javascript
   const BACKEND_URL = 'https://your-backend-url.com';
   ```
3. Билдвайте нов APK с `eas build`

## 🎨 Персонализация

### Смяна на иконата на приложението

1. Заменете файловете в `mobile-app/assets/`:
   - `icon.png` (1024x1024px)
   - `adaptive-icon.png` (1024x1024px)
   - `splash.png` (1284x2778px)

### Смяна на името на приложението

Редактирайте `mobile-app/app.json`:
```json
{
  "expo": {
    "name": "Вашето Име",
    ...
  }
}
```

### Смяна на цветовете

Редактирайте `mobile-app/App.js`, секцията `styles`:
```javascript
const styles = StyleSheet.create({
  header: {
    backgroundColor: '#0066cc', // Смени цвета тук
    ...
  },
  ...
});
```

## 🐛 Troubleshooting

### Приложението не може да се свърже със сървъра

1. Уверете се, че backend сървърът работи
2. Проверете дали телефонът и компютърът са в същата WiFi мрежа
3. Уверете се, че firewall не блокира порт 3001
4. Проверете дали IP адресът в `BACKEND_URL` е правилен

### Грешка при билдване

1. Уверете се, че сте логнати в Expo: `eas login`
2. Проверете дали имате валиден `eas.json` файл
3. Уверете се, че `app.json` е валиден JSON

### Приложението крашва при стартиране

1. Проверете конзолата за грешки: `npx expo start`
2. Уверете се, че всички зависимости са инсталирани: `npm install`
3. Изчистете кеша: `npx expo start -c`

## 📝 Структура на проекта

```
mobile-app/
├── App.js                      # Главен компонент
├── app.json                    # Expo конфигурация
├── package.json                # NPM зависимости
├── eas.json                    # EAS build конфигурация
├── config.js                   # Google Sheets настройки
├── services/
│   └── googleSheets.js         # API service за Google Sheets
├── backend/
│   ├── server.js               # Express backend сървър
│   └── package.json            # Backend зависимости
└── assets/                     # Икони и изображения
```

## 🔒 Сигурност

**ВАЖНО:**
- Service account credentials са включени в backend кода
- НЕ споделяйте backend кода публично
- Разгледайте използването на environment variables за production
- Backend сървърът има само READ-ONLY достъп до Google Sheets

## 📞 Поддръжка

За въпроси и проблеми, моля свържете се с разработчика.

## 📄 Лиценз

Това приложение е създадено за вътрешна употреба.
