# 🚀 Бърз Старт - Lunch Orders App

## За разработчици: Само 5 стъпки до APK файл

### 1️⃣ Инсталирайте зависимостите

```bash
# В mobile-app папката
cd mobile-app
npm install

# В backend папката
cd backend
npm install
cd ..
```

### 2️⃣ Намерете IP адреса на компютъра си

**Windows:**
```bash
ipconfig
```
Търсете "IPv4 Address" (например: 192.168.1.100)

**Mac/Linux:**
```bash
ifconfig
```
или
```bash
hostname -I
```

### 3️⃣ Обновете BACKEND_URL

Отворете `services/googleSheets.js` и променете:
```javascript
const BACKEND_URL = 'http://192.168.1.100:3001'; // Вашият IP тук
```

### 4️⃣ Стартирайте backend сървъра

```bash
cd backend
npm start
```

Оставете този терминал отворен!

### 5️⃣ Билдвайте APK (изберете един от методите)

#### Метод A: С Expo EAS (Препоръчително - най-лесно)

```bash
# В нов терминал в mobile-app папката
npm install -g eas-cli
eas login
eas build:configure
eas build --platform android --profile production
```

След 10-15 минути ще получите линк за изтегляне на APK.

#### Метод B: Локално тестване (преди билд)

```bash
npm start
```

Сканирайте QR кода с "Expo Go" приложението (изтеглете от Play Store).

---

## За крайни потребители: Как да инсталирате приложението

### Стъпка 1: Изтеглете APK файла
- Получете APK файла от разработчика (файл с разширение .apk)

### Стъпка 2: Разрешете инсталация от неизвестни източници
1. Отворете **Настройки** на телефона
2. Намерете **Сигурност** или **Privacy**
3. Включете **Install unknown apps** или **Unknown sources**
4. Изберете файловия мениджър или браузъра от който ще инсталирате

### Стъпка 3: Инсталирайте APK
1. Отворете файловия мениджър
2. Намерете изтегления APK файл (обикновено в папка Downloads)
3. Натиснете върху файла
4. Натиснете **Install**
5. Изчакайте инсталацията да завърши
6. Натиснете **Open** за да отворите приложението

### Стъпка 4: Първо стартиране
1. Приложението ще зареди данните от Google Sheets
2. Използвайте pull-down за refresh
3. Натиснете бутона 🔄 за обновяване на данните

---

## ⚠️ Често срещани проблеми

### Проблем: "Не може да се свърже със сървъра"

**Решение:**
- Уверете се, че backend сървърът работи
- Проверете дали телефонът и компютърът са в същата WiFi мрежа
- За постоянно решение: deploy backend на Render.com или Railway.app

### Проблем: "App not installed"

**Решение:**
- Деинсталирайте старата версия първо
- Уверете се, че APK файлът е изтеглен напълно
- Опитайте да прехвърлите APK през USB кабел

### Проблем: APK не се билдва

**Решение:**
```bash
# Изчистете кеша и опитайте отново
rm -rf node_modules
npm install
eas build --platform android --profile production --clear-cache
```

---

## 🎯 След Production Deploy

Когато deploy-нете backend на облак хостинг:

1. Обновете `services/googleSheets.js`:
```javascript
const BACKEND_URL = 'https://your-app.onrender.com'; // Вашият production URL
```

2. Билдвайте нов APK:
```bash
eas build --platform android --profile production
```

3. Разпространете новия APK на потребителите

---

## 📱 Кратки команди

### Тестване локално
```bash
cd mobile-app
npm start
```

### Билд за production
```bash
eas build -p android --profile production
```

### Проверка на backend
```bash
curl http://localhost:3001/api/health
```

### Изчистване на кеш
```bash
npx expo start -c
```

---

**Готово!** Сега имате работещо Android приложение за обедни поръчки! 🎉
